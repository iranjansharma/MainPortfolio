pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "node --version" 
            }
        }
        stage('Test') { 
            steps {
                echo "node --version"  
            }
        }
        // stage('Deploy') { 
        //     steps {
        //         // 
        //     }
        // }
    }
    }