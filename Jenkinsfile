pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh "node --version" 
            }
        }
        stage('Test') { 
            steps {
                sh "node --version"  
            }
        }
        // stage('Deploy') { 
        //     steps {
        //         // 
        //     }
        // }
    }
    }