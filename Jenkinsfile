pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh "node -v" 
            }
        }
        stage('Test') { 
            steps {
                sh "node -v"  
            }
        }
        // stage('Deploy') { 
        //     steps {
        //         // 
        //     }
        // }
    }
    }